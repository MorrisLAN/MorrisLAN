services:
  cloudflare-tunnel:
    restart: unless-stopped
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    image: cloudflare/cloudflared
    networks:
      tunnel: null
    command: tunnel run

networks:
  tunnel:
    external: false