apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflare-access
data:
  config.yaml: |
    tunnel: "<path:secrets/data/cloudflare/access/tunnels#holo-token>"
    no-autoupdate: true