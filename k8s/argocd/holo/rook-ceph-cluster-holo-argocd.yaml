apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-ceph-cluster-holo
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://charts.rook.io/release
      chart: rook-ceph-cluster
      targetRevision: 1.15.4
      helm:
        valuesObject:
          clusterName: rook-ceph-cluster-holo
          monitoring:
            enabled: true
            createPrometheusRules: true
            rulesNamespaceOverride: kube-prometheus-stack
          cephClusterSpec:
            mgr:
              modules:
                - name: rook
                  enabled: true
            storage:
              useAllNodes: true
              useAllDevices: true
  destination:
    server: https://kubernetes.default.svc
    namespace: rook-ceph
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
