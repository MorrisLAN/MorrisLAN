apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: librespeed-holo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://git.morrislan.net/MorrisLAN/morrislan'
    targetRevision: prod
    path: 'k8s/librespeed/holo'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: librespeed
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true