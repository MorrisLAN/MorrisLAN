apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflare-access-vapor
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'https://git.morrislan.net/MorrisLAN/morrislan'
      targetRevision: prod
      path: 'k8s/cloudflare-access/vapor'
      plugin:
        env:
          - name: AVP_TYPE
            value: vault
          - name: VAULT_ADDR
            value: http://vault.vault.svc:8200
          - name: AVP_AUTH_TYPE
            value: k8s
          - name: AVP_K8S_ROLE
            value: cloudflare-access-vapor
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cloudflare-access
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true